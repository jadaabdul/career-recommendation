## env file
to run this project you need to create .env file in root of backend folder

```env
PORT=5000
DB_HOST=localhost
DB_USER=your user name
DB_PASSWORD=your password
DB_NAME=your database name
JWT_SECRET=career_secret_key
```